# Requirements for CONFIG_SYSTEM_DEVICE modules
# Compatible with Python 3.7+ on NanoPi Duo2

# Web Framework
flask>=1.1.0,<3.0.0
flask-cors>=3.0.0,<5.0.0

# MQTT Communication (compatible with older versions)
paho-mqtt>=1.5.0,<2.0.0

# Network and System Utilities
getmac>=0.8.0
netifaces>=0.10.0
requests>=2.20.0
psutil>=5.6.0

# Task Scheduling
schedule>=1.0.0

# Hardware-specific libraries (for ARM-based boards)
# Note: These may need platform-specific installation
# nanopi.duo  # Uncomment if running on NanoPi Duo
# OPi         # Uncomment if running on Orange Pi

# Development and Testing (optional)
# pytest>=6.0.0
# pytest-cov>=2.0.0